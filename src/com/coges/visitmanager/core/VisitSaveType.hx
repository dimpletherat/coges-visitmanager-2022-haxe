package com.coges.visitmanager.core;


/**
	 * ...
	 * @author Nicolas Bigot
	 */
enum VisitSaveType
{
	FROM_RESULT_SEARCH;
	FROM_VISIT_DETAIL;
	FROM_VISIT_ITEM;
	CHANGE_STATUS;
	FROM_PENDING_VISIT;
}

